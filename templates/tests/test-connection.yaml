apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skyramp-mocker.fullname" . }}-test-connection"
  labels:
    {{- include "skyramp-mocker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skyramp-mocker.fullname" . }}:{{ .Values.service.port }}/readyz']

  restartPolicy: Never
