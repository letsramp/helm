apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skyramp-mocker.fullname" . }}
  labels:
    {{- include "skyramp-mocker.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "skyramp-mocker.bootConfig" . | nindent 4 }}
  resources.yaml: "" 
immutable: false