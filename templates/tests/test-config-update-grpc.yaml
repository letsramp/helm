apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skyramp-mocker.fullname" . }}-test-config-update-grpc"
  labels:
    {{- include "skyramp-mocker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: add-config-map 
      image: radial/busyboxplus:curl
      command: ['curl']
      args: 
        - "-XPUT"
        - "{{ include "skyramp-mocker.fullname" . }}:{{ .Values.service.port }}/skyramp/mocks"
        - "--data-binary"
        - | 
        {{ include "skyramp-mocker-test.grpcPayload" . | nindent 10 }}
  restartPolicy: Never