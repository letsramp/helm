apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongo-persistence.fullname" . }}
  labels:
    {{- include "mongo-persistence.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongo-persistence.selectorLabels" . | nindent 4 }}
