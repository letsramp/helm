apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongo-datastore.fullname" . }}
  labels:
    {{- include "mongo-datastore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongo-datastore.selectorLabels" . | nindent 4 }}
